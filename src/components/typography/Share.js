import styled from "styled-components";

const ShareStyles = styled.span`
	font-size: 18px;
	font-weight: 600;
	text-transform: uppercase;
`;

export const Share = () => <ShareStyles>Share</ShareStyles>;
